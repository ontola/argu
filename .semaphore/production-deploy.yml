version: v1.0
name: Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Deploy
    task:
      secrets:
        - name: deployment-secrets
      jobs:
        - name: Staging
          commands:
            - curl -X POST $DEPLOY_HOOK_PRODUCTION
